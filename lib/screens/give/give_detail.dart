import 'package:flutter/material.dart';

class GiveDetail extends StatelessWidget {
  const GiveDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("GiveDetail");
  }
}
